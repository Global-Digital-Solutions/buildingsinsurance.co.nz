// Metadata is defined inline in the 'use client' component via a workaround:
// Next.js 14+ requires a separate layout or generateMetadata for client components.
// We handle this via a layout file.
export {};
